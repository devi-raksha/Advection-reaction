# Testing

Tests are configured by the top-level CMake project and `tests/CMakeLists.txt`. The test directory includes `DEAL_II_PICKUP_TESTS()`, which discovers deal.II test sources and associates their expected-output files with generated build-tree test executables.

## Configure and run

Configure and build first:

```bash
cmake -S . -B build
cmake --build build
ctest --test-dir build --output-on-failure
```

CTest must be pointed at the build tree: the test executables and generated parameter copies are not in the source tree.

## Current discovery

The audit configuration discovered 12 tests:

- `template.debug`
- `test_constant.debug`
- `test_jacobian_boundary.debug`
- `test_jacobian_cell.debug`
- `test_jacobian_eta.debug`
- `test_jacobian_interior.debug`
- `test_jacobian_junction.debug`
- `test_mass_integration.debug`
- `test_mass_integration_velocity.debug`
- `test_mass_ones.debug`
- `test_trace_residual.debug`
- `test_vtk.debug`

The suffix is generated by the active deal.II build configuration and may differ across build types.

## Recorded status

A repository audit ran `ctest --test-dir build --output-on-failure`: 11 of 12 tests passed, while `tests/test_jacobian_cell.debug` failed its expected-output comparison. The test executable ran, but its numerical output differed from the checked-in expectation. This documentation change does not alter source behavior or expected-output files, so a fresh build should be checked rather than treating the audit result as a permanent diagnosis.

Tests exercise constants, residual/Jacobian pieces, mass integration, trace residuals, and VTK input/output helpers. They are regression checks, not a substitute for model, convergence, or benchmark validation.
